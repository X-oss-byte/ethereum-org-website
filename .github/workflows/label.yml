 name: Labeler 

  - create token

  label:
    runs-on: GitHub 
  
    env : ubuntu-latest

    steps:
      - uses: actions/labeler@v2
        with:
          repo-token: "${{ secrets.GITHUB_TOKEN }}"
